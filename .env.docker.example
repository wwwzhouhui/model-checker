# .env.docker.example
# Docker 部署环境变量模板
# 复制此文件为 .env 并填写实际值

# ============================================
# 必需配置
# ============================================

# JWT 密钥（必须修改）
# 生成命令: openssl rand -base64 64
JWT_SECRET=your-jwt-secret-key-min-64-characters-long-change-this-in-production

# API Key 加密密钥（必须修改）
# 生成命令: openssl rand -hex 32
ENCRYPTION_KEY=your-64-character-hex-string-for-aes-256-gcm-encryption-change-this

# ============================================
# OAuth 配置（可选）
# ============================================

# OAuth 回调地址
# 生产环境需要配置为实际域名
OAUTH_CALLBACK_URL=http://localhost:3000

# GitHub OAuth（可选）
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# LinuxDo OAuth（可选）
LINUXDO_CLIENT_ID=
LINUXDO_CLIENT_SECRET=

# ============================================
# 应用配置
# ============================================

NODE_ENV=production
PORT=3000
